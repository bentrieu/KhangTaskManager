# KhangTaskManager
just to test go + tools integration and maybe cloud <br>
need make + docker to run ts

Use ```make run``` to run 🏃

Use psql cli command <br>
```\c <database_name>``` to connect to database <br>
```\l``` check for existing table
